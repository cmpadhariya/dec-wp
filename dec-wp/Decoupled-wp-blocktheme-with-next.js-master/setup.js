// setup.js

// The URI for the GraphQL endpoint where the application will send GraphQL queries
export const uri = "http://localhost/dwpblock_next/graphql"; 

// The base URL to replace in the content fetched from the WordPress API
export const replaceUrl = "http://localhost/dwpblock_next";

// The current URL where the Next.js application is running
export const currentUrl = "http://localhost:3000";
